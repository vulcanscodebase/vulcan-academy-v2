import { ProfileHeader} from "@/components/(user)/profile-header";
import { ProfileSetup } from "@/components/(user)/profile";

export default function UserProfile(){
    return(
        <>
        <ProfileHeader />
        <ProfileSetup />
        </>
    )
}